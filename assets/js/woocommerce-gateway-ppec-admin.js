jQuery( document ).ready( function( $ ) {
	'use strict';

	// create namespace to avoid any possible conflicts
	$.wc_ppec_admin = {
		init: function() {

		}
	}; // close namespace

	$.wc_ppec_admin.init();

// end document ready
});
